import styled from 'styled-components'
import { ContainerStyled } from 'components/Container/styled'

export const MyLayoutStyled = styled.div`
	display: flex;
	flex-direction: column;
	min-height: 100vh;
`

export const Content = styled(ContainerStyled)``
