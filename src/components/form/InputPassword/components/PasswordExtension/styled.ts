import styled from 'styled-components'
import { colors } from 'utils/constants'

export const PasswordExtensionStyled = styled.div`
	width: 48px;
	height: 48px;
	display: flex;
	align-items: center;
	justify-content: center;
	color: ${colors.greyDark};
	cursor: pointer;
`
